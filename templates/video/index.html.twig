{% extends 'base.html.twig' %}

{% block title %}Bande annonce {{ title }}{% endblock %}

{% block stylesheets %}
    <style>
        nav { margin: 10px 0 }
    </style>
{% endblock %}

{% block body %}

    <h1>Bande annonce {{ title }}</h1>

    <nav>
        <a href="{{ path('video_index', {'slug': 'jurassic-world'}) }}" >Jurassic World 3</a> -
        <a href="{{ path('video_index', {'slug': 'doctor-strange'}) }}">Doctor Strange</a> -
        <a href="{{ path('video_index', {'slug': 'thor'}) }}">Thor</a> -
        <a href="{{ path('video_index', {'slug': 'spider-man'}) }}">Spiderman</a>
    </nav>

    <iframe width="1200" height="630" src="https://www.youtube.com/embed/{{ code }}"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen></iframe>

{% endblock %}
